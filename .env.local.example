# This file is an example. Create a .env.local file with your actual credentials
# or add these variables directly to your Vercel project settings.
# DO NOT COMMIT your .env.local file to Git.

# --- Firebase Public Client-Side Keys ---
# Find these in your Firebase project settings > General > Your apps > Web app
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# --- Firebase Admin Server-Side Keys ---
# Find these in your Firebase project settings > Service accounts > Generate new private key
# This will download a JSON file containing these values.
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# For FIREBASE_PRIVATE_KEY, copy the entire key from the JSON file,
# including the "-----BEGIN PRIVATE KEY-----" and "-----END PRIVATE KEY-----" lines.
# When adding to Vercel, it's best to paste it directly into the value field.
FIREBASE_PRIVATE_KEY=

# --- EmailJS Keys ---
# Find these in your EmailJS account dashboard.
NEXT_PUBLIC_EMAILJS_SERVICE_ID=service_jmkr2dn
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=dh8MnuS1CHuhkCk4X
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=assessment_template
